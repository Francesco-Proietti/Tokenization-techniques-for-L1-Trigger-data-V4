# Default trainer config

# Number of epochs
max_epochs: 10

# Batch size
batch_size: 64

# Learning rate
lr: 1e-4

# Accelerator
accelerator: auto

# Device
devices: auto

# How many logs
log_every_n_steps: 10

# Seed for reproducibility
seed: 56